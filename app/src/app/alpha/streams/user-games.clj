(ns app.alpha.streams.user-games)


(comment
  
  ; ktable of :u/uuid -> (list-of :g/uuids )
  ; request for list of user games will read from the store using uuids
  ; client will sort/group by host/player/observer
  
  ;;
  )