{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Death Star",
			"type": "extensionHost",
			"request": "launch",
			"runtimeExecutable": "${execPath}",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}/build"]
		}
	]
}
