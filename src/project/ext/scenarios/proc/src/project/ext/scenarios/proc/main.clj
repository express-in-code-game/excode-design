(ns project.ext.scenarios.proc.main
  (:require
   [project.ext.scenarios.meta.protocols :as scenarios.p]
   [project.scenario.rovers1.main :as scenario.rovers1.main]))

(defn -main [& args]
  (prn 'project.ext.scenarios.proc.main))
